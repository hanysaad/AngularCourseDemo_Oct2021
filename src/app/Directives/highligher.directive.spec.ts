import { HighligherDirective } from './highligher.directive';

describe('HighligherDirective', () => {
  it('should create an instance', () => {
    const directive = new HighligherDirective();
    expect(directive).toBeTruthy();
  });
});
